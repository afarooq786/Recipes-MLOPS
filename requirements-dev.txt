# Dev/CI-only dependencies -- testing and monitoring tools that don't need
# to ship in the production API/training image. Installed alongside
# requirements.txt for local development and in CI.

pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
evidently>=0.7
requests>=2.31
streamlit==1.48.0
